import { Translations } from './de';

const decimal = (value: number) => value.toLocaleString('en-US', { maximumFractionDigits: 1 });

export const en: Translations = {
  app: {
    name: 'Workout Helper',
  },
  common: {
    back: 'Back',
    cancel: 'Cancel',
    save: 'Save',
    undo: 'Undo',
    dismiss: 'Dismiss',
    decrease: (label: string) => `Decrease ${label.toLowerCase()}`,
    increase: (label: string) => `Increase ${label.toLowerCase()}`,
  },
  units: {
    decimal,
    secondsShort: (value: number) => `${decimal(value)} s`,
    seconds: (value: number) => (value === 1 ? '1 second' : `${decimal(value)} seconds`),
    duration: (totalSeconds: number) => {
      const minutes = Math.floor(totalSeconds / 60);
      const seconds = totalSeconds % 60;
      const parts: string[] = [];
      if (minutes > 0) {
        parts.push(minutes === 1 ? '1 minute' : `${minutes} minutes`);
      }
      if (seconds > 0 || minutes === 0) {
        parts.push(seconds === 1 ? '1 second' : `${seconds} seconds`);
      }
      return parts.join(' ');
    },
  },
  pageTitles: {
    home: 'Overview',
    settings: 'Settings',
    timer: 'Advanced Timer',
    presets: 'Favorites & history',
    run: 'Workout',
  },
  home: {
    newTimer: 'New timer',
    empty: 'No timers yet. Create your first one with “New timer”.',
  },
  settings: {
    heading: 'Settings',
    language: 'Language',
    languageHint: 'Timer callouts are played in the selected language.',
    theme: 'Appearance',
    themeOptions: {
      system: 'Match device',
      light: 'Light',
      dark: 'Dark',
    },
    accent: 'Color scheme',
    accentOptions: {
      red: 'Ring red',
      ocean: 'Ocean',
      forest: 'Forest',
      violet: 'Violet',
      ember: 'Ember',
      graphite: 'Graphite',
    },
  },
  timer: {
    heading: 'Advanced Timer',
    presetsLink: 'Favorites & history',
    roundsSection: 'Rounds',
    rounds: 'Number of rounds',
    roundsText: (rounds: number) => (rounds === 1 ? '1 round' : `${rounds} rounds`),
    work: 'Round duration',
    rest: 'Break',
    prep: 'Get ready',
    timeline: (summary: string, total: string) => `Sequence: ${summary}. Total ${total}.`,
    structureSection: 'Intervals & countdown',
    interval: 'Interval within round',
    intervalHint: 'Splits every round into equal parts, with a signal at each switch.',
    intervalOption: (time: string, count: number) => `${time} (${count} parts)`,
    intervalUnfit: (time: string) => `${time} (doesn’t fit)`,
    intervalFit: 'The interval must divide the round duration evenly. Pick another interval.',
    countdown: 'Countdown',
    countdownHint: 'Counts down the last seconds of every round, break and the get-ready phase.',
    countdownValue: (seconds: number) => (seconds === 0 ? 'Off' : `${seconds} s`),
    countdownText: (seconds: number) =>
      seconds === 0 ? 'Off' : seconds === 1 ? '1 second' : `${seconds} seconds`,
    off: 'Off',
    calloutsSection: 'Callouts',
    calloutsToggle: 'Play callouts',
    calloutsHint: 'The time between two sounds is picked randomly from this range.',
    calloutsOffHint: 'Only the signals for rounds, intervals and the countdown will play.',
    minGap: 'Minimum gap',
    maxGap: 'Maximum gap',
    soundsSection: 'Sounds',
    noSounds: 'Select at least one sound.',
    signalsSection: 'Signals',
    signals: {
      roundStart: 'Round start',
      interval: 'Interval switch',
      countdown: 'Countdown',
      roundEnd: 'Round end',
      finish: 'Workout end',
    },
    previewSignal: (label: string) => `Play ${label.toLowerCase()} sound`,
    total: (duration: string) => `Total ${duration}`,
    saveFavorite: 'Favorite',
    start: 'Start',
    favoriteSaved: (name: string) => `Saved “${name}” as a favorite.`,
  },
  sounds: {
    categories: {
      numbers: 'Numbers',
      colors: 'Colors',
      directions: 'Directions',
      signals: 'Signals',
    },
    selectedCount: (selected: number, total: number) => `${selected} of ${total}`,
    selectAll: 'All',
    selectAllLabel: (group: string) => `Select all ${group.toLowerCase()}`,
    selectNone: 'None',
    selectNoneLabel: (group: string) => `Select none of the ${group.toLowerCase()}`,
    none: 'No sound',
    spokenCountdown: 'Spoken numbers',
  },
  run: {
    prep: 'Get ready',
    work: 'Round',
    rest: 'Break',
    roundOf: (round: number, total: number) => `Round ${round} of ${total}`,
    nextRound: (round: number, total: number) => `Up next: round ${round} of ${total}`,
    intervalOf: (interval: number, total: number) => `Interval ${interval} of ${total}`,
    nextSwitch: (time: string) => `Switch in ${time}`,
    totalRemaining: (time: string) => `${time} left in total`,
    loading: 'Loading sounds…',
    pause: 'Pause',
    resume: 'Resume',
    paused: 'Paused',
    skip: 'Skip',
    stop: 'Stop',
    finished: 'Done!',
    finishedDetail: (rounds: number) =>
      rounds === 1 ? '1 round completed.' : `${rounds} rounds completed.`,
    again: 'Again',
    adjust: 'Adjust timer',
  },
  presets: {
    heading: 'Favorites & history',
    favorites: 'Favorites',
    noFavorites: 'No favorites yet. Save a setup in the timer using “Favorite”.',
    history: 'Recently used',
    noHistory: 'Your last 10 started workouts will show up here.',
    summary: {
      rest: (time: string) => `Break ${time}`,
      interval: (time: string) => `every ${time}`,
      callouts: (minGap: number, maxGap: number, sounds: number) =>
        `${decimal(minGap)}–${decimal(maxGap)} s · ${sounds === 1 ? '1 sound' : `${sounds} sounds`}`,
      noCallouts: 'no callouts',
    },
    edit: (name: string) => `Edit “${name}”`,
    delete: (name: string) => `Delete “${name}”`,
    deleted: (name: string) => `Deleted “${name}”.`,
    saveTitle: 'Save as favorite',
    editTitle: 'Edit favorite',
    nameLabel: 'Name',
    nameRequired: 'Please enter a name.',
    descriptionLabel: 'Description (optional)',
    characterCount: (count: number, max: number) => `${count} of ${max} characters`,
    groupLabel: 'Group',
    noGroup: 'No group',
    newGroupOption: 'New group…',
    newGroupNameLabel: 'Name of the new group',
    groupNameLabel: 'Group name',
    groupNameRequired: 'Please enter a group name.',
    ungrouped: 'Without group',
    emptyGroup: 'No favorites in this group yet.',
    addGroup: 'New group',
    createGroupTitle: 'Create group',
    renameGroupTitle: 'Rename group',
    renameGroup: (name: string) => `Rename group “${name}”`,
    deleteGroup: (name: string) => `Delete group “${name}”`,
    groupDeleted: (name: string) => `Deleted group “${name}”, its favorites were kept.`,
    defaultName: (rounds: number, work: string) => `${rounds} × ${work}`,
  },
};
